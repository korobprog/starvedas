# Project-local Codex config intentionally left empty.
# Dokploy MCP is configured globally in C:\Users\makst\.codex\config.toml.
# Keeping a duplicate MCP server here caused an extra MCP startup only in StarVedas.